# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mvcvs?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=shivanshshivanshshivanshshivanshshivansh
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173

# Anthropic API Configuration (leave empty to use predefined scenarios)
anthropic.api.key=

# Groq API Configuration (free, fast AI)
groq.api.key=

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.mvcvs=DEBUG

